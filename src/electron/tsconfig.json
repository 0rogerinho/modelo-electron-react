{
  "compilerOptions": {
    // require strict types (null-save)
    "strict": true,
    // tell Typescript to generate ESM Syntax
    "target": "ESNext",
    // tell Typescrip to require ESM Syntax as input (including .js file imports)
    "module": "NodeNext",
    // define where to up generated JS
    "outDir": "../../dist-electron",
    // ignore errors from dependencies
    "skipLibCheck": true,
    // global types
    "types": ["../../types"]
  }
}
